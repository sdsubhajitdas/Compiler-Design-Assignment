#include<stdio.h>
#include<stdlib.h>

void main()
{
	int a=2,b=0,c;
    if( !c)
    {
    	c=0;
    	printf("Value of the variable after initialization=%d",c);
    }
    else
    {
    	printf("Value of the variable is=%d",c );
    }
}